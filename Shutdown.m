% shutdown.m - очистка при закрытии проекта HT1
fprintf('====================================\n');
fprintf('Закрытие проекта HT1...\n');
fprintf('====================================\n');

% 1. Сохраняем важные данные (опционально)
try
    if exist('Fx', 'var') && exist('Fe', 'var')
        save('last_system_state.mat', 'Fx', 'Fe', '-v7.3');
        disp('Состояние системы сохранено в last_system_state.mat');
    end
catch
    % Игнорируем ошибки при сохранении
end

% 2. Очищаем workspace (осторожно!)
% clear variables  % Раскомментировать если нужно

% 3. Закрываем все графики
close all;

clear all;

% 4. Очищаем командное окно
% clc;  % Раскомментировать если нужно

disp('Проект HT1 закрыт.');
fprintf('====================================\n');